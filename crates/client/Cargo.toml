[package]
name = "client"
edition.workspace = true
version.workspace = true
rust-version.workspace = true

[dependencies]
proto = { path = "../proto" }
netchan = { path = "../netchan" }
auth = { path = "../auth" }
bot = { path = "../bot" }
