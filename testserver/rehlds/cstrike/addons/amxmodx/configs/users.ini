; Users configuration file — hardened by counter-strike-boost.com
; File location: $moddir/addons/amxmodx/configs/users.ini
;
; ---------------------------------------------------------------------------
; WHAT WE CHANGED
; ---------------------------------------------------------------------------
; AMX Mod X ships this file with one active line:
;
;     "loopback" "" "abcdefghijklmnopqrstuv" "de"
;
; It grants EVERY admin flag - including rcon access - to any connection the
; server treats as loopback, with no password. If anything ever proxies or
; forwards players through localhost, that is full admin for everybody.
;
; We have commented it out. Add yourself properly using the examples below.
; ---------------------------------------------------------------------------

; Access flags:
; a - immunity (can't be kicked/banned/slayed/slapped and affected by other commmands)
; b - reservation (can join on reserved slots)
; c - amx_kick command
; d - amx_ban and amx_unban commands (permanent and temporary bans)
; e - amx_slay and amx_slap commands
; f - amx_map command
; g - amx_cvar command (not all cvars will be available)
; h - amx_cfg command
; i - amx_chat and other chat commands
; j - amx_vote and other vote commands
; k - access to sv_password cvar (by amx_cvar command)
; l - access to amx_rcon command and rcon_password cvar (by amx_cvar command)
; m - custom level A (for additional plugins)
; n - custom level B
; o - custom level C
; p - custom level D
; q - custom level E
; r - custom level F
; s - custom level G
; t - custom level H
; u - menu access
; v - amx_ban and amx_unban commands (temporary bans only)
; z - user (no admin)

; Account flags:
; a - disconnect player on invalid password
; b - clan tag
; c - this is steamid/wonid
; d - this is ip
; e - password is not checked (only name/ip/steamid needed)
; k - name or tag is case sensitive

; Password:
; Add to your autoexec.cfg: setinfo _pw "<password>"
; Change _pw to the value of amx_password_field

; Format of admin account:
; <name|ip|steamid> <password> <access flags> <account flags>

; ---------------------------------------------------------------------------
; ADD YOUR ADMINS HERE
; ---------------------------------------------------------------------------
; Recommended - match on SteamID, which cannot be spoofed:
; "STEAM_0:0:123456" "" "abcdefghijklmnopqrstuv" "ce"
;
; Password-based, works for non-Steam players too:
; "My Name" "my_password" "abcdefghijklmnopqrstuv" "a"
;
; By IP - only if the address is genuinely static and yours:
; "123.45.67.89" "" "abcdefghijklmnopqrstuv" "de"

; Disabled by counter-strike-boost.com - see the note at the top of this file.
; "loopback" "" "abcdefghijklmnopqrstuv" "de"
