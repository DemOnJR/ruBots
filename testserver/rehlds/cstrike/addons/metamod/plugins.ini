; Trimmed for protocol testing.
;
; The full CSB stack segfaults on boot here: AMX Mod X ships an empty
; modules.ini while its plugins.ini expects CSX/MySQL etc., so the plugin
; suite loads against modules that are not there. None of that matters for
; testing the bot's netcode -- what does matter is the ENGINE and AUTH path a
; real public server uses, which is ReHLDS + ReGameDLL + Metamod-R + Reunion.
;
; plugins.ini.full is the original if the whole suite is ever needed.
linux addons/reunion/reunion_mm_i386.so
